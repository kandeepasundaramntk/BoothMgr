# dependencies / build
node_modules/
dist/
.vite/
*.tsbuildinfo

# environment & secrets — Supabase keys live here, never commit
.env
.env.local
.env.*.local

# real voter data must never be committed (see CLAUDE.md — Data Sensitivity)
# anchored to the repo root: src/data/ is application code and must be tracked
/data/
*.csv
exports/

# editor / OS
.DS_Store
~$*
*.local
.vscode/
.idea/

# logs
npm-debug.log*
*.log

# scratch state for subagent-driven-development execution
.superpowers/
